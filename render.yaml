services:
  - type: web
    name: backstage-portal
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile   # root Dockerfile for Backstage
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 7007
